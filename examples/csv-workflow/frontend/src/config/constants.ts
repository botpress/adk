export const BOT_CONFIG = {
  name: "CSV Import Assistant",
  avatar: "",
  description: "Workflow ↔ Conversation communication demo — import CSV files with interactive validation and error resolution.",
} as const;

export const CLIENT_ID = import.meta.env.VITE_CLIENT_ID as string;
